# PII-gate ALLOWLIST — tokens the pre-commit scanner must IGNORE.
#
# ╔════════════════════════════════════════════════════════════════════════╗
# ║  FALSE POSITIVES ONLY.  NEVER put real PII here.                        ║
# ║  Only PUBLIC or SYNTHETIC tokens belong in this file — it is COMMITTED. ║
# ║  Real names / emails / customers / employer / internal codenames must   ║
# ║  NEVER be allowlisted; put those in .pii-denylist.local.txt (gitignored)║
# ║  so the gate ALWAYS blocks them.                                        ║
# ╚════════════════════════════════════════════════════════════════════════╝
#
# One token per line. Blank lines and lines starting with '#' are ignored.
#
# TWO MATCHING MODES, both handled by the hook:
#   - Stage 1 (deterministic secret/API-key gate): EXACT whole-token match,
#     case-insensitive. An entry only ever exempts that one literal token.
#   - Stage 2 (semantic `claude -p` pass): case-insensitive substring.
#
# ⚠  EVERY ENTRY IS A HOLE IN THE GATE. An entry here is a token this repo
#    asserts is SYNTHETIC and non-resolving. Adding a REAL credential — or a
#    real name, address, or customer reference — does not "silence a false
#    positive": it permanently defeats the gate for that value, in a file that
#    is itself committed and public. If you are not certain a value is fake,
#    it does not belong here. Real sensitive terms go in the gitignored
#    .pii-denylist.local.txt so the gate ALWAYS blocks them.

# --- Synthetic placeholders (standard fictional names) -------------------
Acme
AcmePlatform
Contoso
Northwind
Sidekick
Jane Doe
John Doe

# --- Scrubbed spec-owner persona (docs/specs/) ---------------------------
# The stand-in the PII scrub substituted for the real document owner. It is a
# synthetic name and resolves to nobody. Listed because docs/specs/ became
# tracked (see the .gitignore note on rule 11b) and the persona appears there
# ten times — five as the `**Owner:**` / `**Author:**` header of a design doc,
# five inside 2026-04-29-metadata-aware-linker-design.md's worked examples,
# where it stands in for the corpus owner beside the already-redacted
# redacted@example.com. Its synthetic siblings (Jane Doe, the demo cast) were
# allowlisted; this one was left out, so the gate treats identical scrub output
# two different ways depending on which placeholder the scrub happened to pick.
Pat Morgan

# --- `brain demo` synthetic cast + companies (invented — see src/brain/demo) --
# The zero-Ollama demo corpus is 100% fictional. These names/companies are
# invented for the demo world and must never be confused with real PII.
Sam Rivera
Priya Okafor
Marcus Chen
Dana Whitfield
Jordan Alvarez
Riley Nakamura
Larkspur
Vendra
Meridian

# --- bin/brain-wiki-gif synthetic corpus (rendered-wiki GIF) -------------
# The wiki-GIF demo vault is 100% fictional (Larkspur compliance universe,
# same cast as `brain demo`; emails use the synthetic example.com domain).
# Jordan Alvarez / Sam Rivera / Larkspur are already listed above.
Priya Chandra

# --- Synthetic credential fixtures (tests/secret_fixtures.py) ------------
# The ingest secret guard (F4) needs credential-SHAPED strings to test against.
# Every value below is fake and non-resolving: the AWS pair are Amazon's own
# published documentation examples, and the rest spell EXAMPLE / NOT / A / REAL
# in the entropy positions where a genuine credential would carry secret bytes.
# None authenticates against any service.
#
# These are matched exact-line and case-insensitively by the hook's stage-1
# gate. Adding a NEW fixture to tests/secret_fixtures.py means adding its token
# here too, or the commit that introduces it will be blocked — which is the
# gate working as intended.
AKIAIOSFODNN7EXAMPLE
ASIAIOSFODNN7EXAMPLE
# NEVER put a real PEM banner here (RSA / EC / DSA / OPENSSH / ENCRYPTED /
# bare PRIVATE KEY). Entries are subtracted from the hit list VERBATIM, and on
# a pasted private key the banner is the ONLY part that matches any pattern —
# the base64 body matches nothing. Allowlisting a real banner therefore empties
# the hit list for a genuine key and the gate never fires. This entry is a
# synthetic banner that no real key can have; keep it that way.
-----BEGIN EXAMPLE NOT A REAL PRIVATE KEY-----
xoxb-EXAMPLE-NOT-A-REAL-TOKEN-000000
sk-EXAMPLENOTAREALKEY0000000000
sk-proj-EXAMPLE-NOT-A-REAL-KEY-000000
sk_live_EXAMPLENOTAREAL000000
rk_live_EXAMPLENOTAREAL000000
ghp_EXAMPLENOTAREALTOKEN0000000000000000
github_pat_EXAMPLE_NOT_A_REAL_TOKEN_0000
glpat-EXAMPLE-NOT-REAL-0000
AIzaEXAMPLENOTAREALGOOGLEAPIKEY00000000

# --- Synthetic credential illustrations in the F4-F6 secret-guard spec ---
# docs/specs/2026-07-25-sections/F4-F6-ingest-secret-guard.md is a spec ABOUT
# the ingest secret guard, so it must quote credential-SHAPED strings to explain
# what the guard matches. Both values below are degenerate by construction —
# they carry the pattern's fixed prefix followed by filler in exactly the
# positions where a genuine credential carries its secret bytes, so neither can
# authenticate against anything:
#   - the AWS-shaped one is the `AKIA` prefix + 16 identical letters
#   - the Stripe-shaped one is the `sk_live_` prefix + the alphabet in sequence
# The spec itself (§7 risk table) states that fixtures like these belong here.
AKIAAAAAAAAAAAAAAAAA
sk_live_ABCDEFGHIJKLMNOPQRSTUV

# --- Invented persona names used in spec examples ------------------------
# Minted for spec prose/fixtures during the docs/specs PII scrub; they refer to
# no real person, and their example addresses use the RFC 2606 example.com
# domain. Listed here so the semantic pass — whose prompt names only a fixed
# cast of placeholders — recognises them as synthetic.
Jordan Vale
Rowan Pike

# --- Public / generic terms confirmed safe (not the user's PII) ----------
# Known false positives that surfaced from the corpus as generic public terms.
NFPA
Neon
DACs
Sdks
API
