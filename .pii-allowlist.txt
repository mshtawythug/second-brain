# PII-gate ALLOWLIST — tokens the pre-commit scanner must IGNORE.
#
# ╔════════════════════════════════════════════════════════════════════════╗
# ║  FALSE POSITIVES ONLY.  NEVER put real PII here.                        ║
# ║  Only PUBLIC or SYNTHETIC tokens belong in this file — it is COMMITTED. ║
# ║  Real names / emails / customers / employer / internal codenames must   ║
# ║  NEVER be allowlisted; put those in .pii-denylist.local.txt (gitignored)║
# ║  so the gate ALWAYS blocks them.                                        ║
# ╚════════════════════════════════════════════════════════════════════════╝
#
# One token per line. Blank lines and lines starting with '#' are ignored.
# Matching is case-insensitive substring (handled by the hook).

# --- Synthetic placeholders (standard fictional names) -------------------
Acme
AcmePlatform
Contoso
Northwind
Sidekick
Jane Doe
John Doe

# --- `brain demo` synthetic cast + companies (invented — see src/brain/demo) --
# The zero-Ollama demo corpus is 100% fictional. These names/companies are
# invented for the demo world and must never be confused with real PII.
Sam Rivera
Priya Okafor
Marcus Chen
Dana Whitfield
Jordan Alvarez
Riley Nakamura
Larkspur
Vendra
Meridian

# --- bin/brain-wiki-gif synthetic corpus (rendered-wiki GIF) -------------
# The wiki-GIF demo vault is 100% fictional (Larkspur compliance universe,
# same cast as `brain demo`; emails use the synthetic example.com domain).
# Jordan Alvarez / Sam Rivera / Larkspur are already listed above.
Priya Chandra

# --- Public / generic terms confirmed safe (not the user's PII) ----------
# Known false positives that surfaced from the corpus as generic public terms.
NFPA
Neon
DACs
Sdks
API
