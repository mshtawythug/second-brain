"""GraphRAG test support package (wave G1-d).

Holds reusable test infrastructure that is imported by test modules rather than
collected as tests itself — currently the synthetic-graph benchmark generator
(:mod:`tests.graphrag.benchmark_fixture`) that G2's P95 perf gate drives at full
scale. Importable as ``tests.graphrag.*`` (``tests`` is a package).
"""
